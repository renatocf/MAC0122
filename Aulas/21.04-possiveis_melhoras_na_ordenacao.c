/*	Podemos ver algumas possíveis melhoras nos algoritmos de ordenação
	já mostrados (SELEÇÃO e INSERÇÃO)
	
	INSERÇÃO
	Em vez de percorrermos o vetor todo para ver onde o colocaríamos,
	poderíamos usar a BUSCA BINÁRIA para achar a posição em que o ele-
	mento possivelmente estará (o que torna o algoritmo muito mais
	rápido, pois trocamos uma busca O(n) por uma O(ln n). Porém, o 
	inserction sort não deixa de ser O(n²) (mas mais rápido).
*/
